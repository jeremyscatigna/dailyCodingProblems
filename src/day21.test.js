import { minimumNumberOfRooms } from "./day21";

describe("day4: find the first missing positive integer", () => {
	const input = [[30, 75], [0, 50], [60, 150]];
	const expected = 2;

	it(`should return the minimum number of rooms required: '${input}'`, () => {});
});
